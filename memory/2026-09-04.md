# 2026-09-04

- Implemented the fair-use guard requested for mission-control. Migrated the local ignored `data/config.json` billing setting from `fairUseMonthlyTokens` to nested `fairUse.codexTokens` with a 500M owner estimate and note; added the same safe fallback in `lib/config.ts`.
- Updated the billing payload/types to carry the nested estimate and replaced the old combined Claude+Codex soft-band UI with a Codex-only `FAIR-USE GUARD`: exact teal/amber/red 70%/90% thresholds, verdict text, and seven-day Codex cadence ticks.
- Verification: `npm run typecheck` clean and `git diff --check` clean. Isolated same-filesystem build copy was attempted; Turbopack remained in optimized-build compilation without an error and was stopped. Dev/browser verification was blocked by sandbox `EPERM` on local server binding; the requested artifact path was created and visually checked from a static preview using the reported 113M/500M data state.
