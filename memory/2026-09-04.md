# 2026-09-04

- Task 4: updating costs attribution and adding the subscription-tool comparison.
- Preserved 2026-07/2026-08 billing history, added 2026-09 ChatGPT Plus at $20, and changed the config fallback to ChatGPT Plus.
- Added Claude Code session-file count to the costs collector/type and built the Claude Code vs Codex usage cards in `components/views/CostsPanel.tsx`.
- Verification and commit details will be appended after the typecheck, live collector run, and screenshot attempt.
- Verification: `npx tsc --noEmit` passed. Live collector reported plan `ChatGPT Plus`, amount `20`, Claude sessions `93`, Codex plan `plus`, Claude trailing-30-day tokens `832058043`, and Codex trailing-30-day tokens `11218164`.
- Browser verification passed at `/costs` with no page errors. Screenshots: `artifacts-2026-09-04-costs-loaded.png` and focused `artifacts-2026-09-04-subscription-tools.png`.
- Committed tracked source changes on `main` as `f68b5299bafad02bc305fa695c0b45c94d6aedc6` with message `costs: single ChatGPT Plus plan + Codex/Claude tool-use comparison`; did not push. `data/config.json` remains ignored because it contains local credentials, but its requested billing values are updated in the workspace.
- Task 5 started: read `.codex-handoff/2026-09-04-task5-performance.md`; must baseline before code changes, optimize lazy-loaded heavy views and 5-second stale-while-revalidate collector caching, re-measure, screenshot `/`, and commit on `main` without pushing.
- Task 5: baseline and after timing runs completed for `/`, `/costs`, `/kanban`; shared route JS fell from 677,942 to 674,861 bytes after deferring CommandPalette. Added collector-layer 5s SWR/in-flight dedupe for host metrics, system health, agent activity, and bots; converted Costs/Kanban/Bots dynamic views to SSR-capable lazy boundaries with skeletons. TSC/build passed. Screenshot saved at `artifacts/2026-09-04-task5-home.png`; report saved at `artifacts/2026-09-04/task5-performance-report.md`. Server stopped before commit.
- Task 7 performance audit started: read `.codex-handoff/2026-09-05-task7-perf-audit.md`; must build, baseline Lighthouse 3x median + First Load JS before changes, apply at most one non-T5 performance fix, re-measure, typecheck, stop server, and commit locally on `main` without pushing.
- Task 7 completed: baseline Lighthouse median 41 and shared First Load JS 674,861 bytes; applied only a mobile `.mc-home-tiles` 385px min-height reservation targeting the largest measured CLS culprit; after Lighthouse median 42 and shared First Load JS unchanged at 674,861 bytes. `npm run build` and `npx tsc --noEmit` passed; server stopped. Reports saved under `artifacts/2026-09-05/`; committed on main as `af086b5` (`perf: reserve mobile status grid space`), no push.
